.claude/
build-zip.ps1

# Python QA cache
__pycache__/
*.pyc

# Only Blue Lemonade is distributed from this repository.
# The website (docs/) is the gh-pages branch, checked out here as a separate worktree
/docs/

# 개발 원본 · 개인 메모는 공개 저장소로 나가지 않는다 (4.5.6).
# 같은 내용이 .git/info/exclude 에도 있지만 그 파일은 clone · 백업에 따라오지 않는다 —
# 다른 기기에서 이 저장소를 받아 작업하면 보호가 없어진다. 그래서 여기에도 둔다.
/salty-ext/
/.handoff/
/AGENTS.md
/CLAUDE.md
